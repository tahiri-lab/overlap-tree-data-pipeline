# Constructed datasets of overlapping phylogenetic trees

This directory contains 2 types of datasets:

- **`datasets-mode1/`**  
  Overlapping tree datasets constructed using mode 1

- **`datasets-mode2/`**  
  Overlapping tree datasets constructed using mode 2
